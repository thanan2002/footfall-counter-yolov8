# Auto-generated ByteTrack config (compatible with Ultralytics 8.3+)
tracker_type: bytetrack

# Core thresholds
track_high_thresh: 0.35   # default ~0.5
track_low_thresh: 0.10    # default ~0.1
new_track_thresh: 0.25    # default ~0.4

# Association / buffering
match_thresh: 0.7         # default ~0.8
track_buffer: 50          # keep tracks a bit longer
min_box_area: 10
mot20: false
